# AI Detector & Humanizer Configuration
# Copy this file to .env and fill in your values

# Router API Configuration (Required for production)
ROUTER_API_KEY=your_router_api_key_here
ROUTER_API_URL=https://api.router.example.com/v1/completions

# Application Configuration
NODE_ENV=development
PORT=5000

# Session Configuration
SESSION_SECRET=your_session_secret_here

# Development Notes:
# - Without ROUTER_API_KEY, the app will run in dev mode with mock responses
# - Replace the ROUTER_API_URL with your actual Router endpoint
# - Generate a secure SESSION_SECRET for production use
